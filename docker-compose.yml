version: '3.8'

services:
  ollama-mcp-bridge:
    build:
      context: .
      dockerfile: mcp-bridge.Dockerfile
    working_dir: /app
    volumes:
      - ./mcp-config.json:/app/mcp-config.json
    environment:
      PYTHONUNBUFFERED: 1
      SYSTEM_PROMPT: >
        You are a helpful and concise assistant with access to external tools.
        The tool schemas are defined for you.
        You should use these tools proactively whenever they can help answer a user's question.
        When a tool returns information, use it to formulate a clear, human-readable answer for the user.
    command: >
      sh -c "ollama-mcp-bridge --ollama-url http://host.docker.internal:11434 --host 0.0.0.0"
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    depends_on:
      model-puller:
        condition: service_completed_successfully
      mcp-rag-vector:
        condition: service_healthy
    restart: always

  # Helper service to pull the all-minilm model automatically on startup
  model-puller:
    image: curlimages/curl
    command: >
      sh -c "echo 'Pulling all-minilm embedding model...';
             curl -X POST http://host.docker.internal:11434/api/pull -d '{\"name\": \"all-minilm\"}';
             curl -X POST http://host.docker.internal:11434/api/pull -d '{\"name\": \"glm4:latest\"}';
             echo 'Model pull request sent.'"
  
  mcp-rag-vector:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    command: ./server server
    environment:
      OLLAMA_HOST: http://host.docker.internal:11434
      OLLAMA_EMBEDDING_MODEL: all-minilm
      QDRANT_HOST: qdrant:6334
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/checks/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    
    restart: always

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: always

volumes:
  ollama_data:
  qdrant_data:
